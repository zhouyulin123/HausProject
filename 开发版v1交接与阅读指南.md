# Haus 开发版 v1 交接与阅读指南

## 本轮交付范围

本轮以开发版功能闭环为终点：自然语言需求、方案生成、现有家具搭配、确定性报价、房间三维编辑、开放几何家具定制、版本恢复与交付。暂停新增功能，先阅读代码和熟悉现有流程。

开发商品使用现有 40 件活动家具，库存、地区及有效期等补充字段属于模拟数据；不是经过厂家商业核验的商品。开放几何是可编辑概念模型，不是制造级 CAD。原阶段 4 的真实业务验收仍需真实授权案例、人工标注、商业商品和受控评测环境，不在本次开发版完成声明中。

## 先运行，再读代码

1. 先阅读 `README.md` 的环境配置和启动部分；本机可运行 `startHaus.bat --check` 确认数据库迁移、应用导入和模型配置。
2. 执行 `startHaus.bat` 启动完整开发环境。模型代理、MySQL、Blender 和效果图服务须按所使用的功能准备；只有 API 进程不足以消费后台生成任务。
3. 打开 `http://127.0.0.1:8080/design/new`，选择商品搭配或家具定制。旧匿名任务与浏览器会话绑定，不能把别人的任务 URL 当成公开演示链接。
4. 先走下方三个案例，再结合任务时间线阅读请求和状态，不必从全部测试文件开始。

## 建议演示案例

### 需求逐轮补充

- 第一轮：我想重新布置客厅，喜欢清爽一些。
- 第二轮：预算最多两万元，客厅宽 4.2 米、长 5.5 米、层高 2.8 米，在中国使用，喜欢现代简约。
- 检查：不再重复追问已明确的预算和尺寸；方案生成进入后台队列。生成完成后检查家具清单、服务端报价和三维场景。

### 修改与撤回

- 第一轮：设计客厅，预算最多两万元，宽 4.2 米，层高 2.8 米，现代简约，长度和配送地区还没确定。
- 第二轮：刚才的预算作废，其他已确认需求保留。
- 第三轮：宽度量错了，实际是四百五十厘米。
- 检查：宽度为 4.5 米，预算仍待确认，层高和风格保留。刷新后仍是当前需求，不恢复已撤回的预算。

### 家具多轮编辑

- 第一轮：设计一张圆形边几，高 500 毫米，台面直径 450 毫米，浅木色台面和黑色圆柱底座。
- 第二轮：整体只加宽百分之十，颜色、高度和深度保持原样。
- 第三轮：只把台面改成墨绿色。
- 检查：保留部件身份和未修改参数；撤销恢复旧版本。手机端切换到“3D”查看模型。
- 超出当前几何工具能力的 NURBS 或任意布尔请求应明确拒绝，并保留原模型。

## 推荐阅读顺序

| 顺序 | 代码位置 | 重点问题 |
| --- | --- | --- |
| 1 | `frontend/src/pages/DesignStartPage.tsx`、`DesignWorkspacePage.tsx` | 如何创建任务、组织对话与三维视图？ |
| 2 | `frontend/src/api/designApi.ts`、`backend/app/api/routes/design_agent.py` | 会话、turn 幂等键和版本如何传递？ |
| 3 | `backend/app/agents/design_agent.py` | 澄清、执行、校验、审批之间如何迁移？ |
| 4 | `backend/app/services/design_agent_service.py`、`agent_fact_extraction_service.py` | 自然语言如何变成有证据的事实补丁并持久化？ |
| 5 | `backend/app/services/catalog_service.py`、`product_eligibility.py`、`layout_service.py` | 哪些业务判断不用大模型完成？ |
| 6 | `backend/app/services/open_geometry_service.py`、`backend/app/schemas/open_geometry.py` | 如何保留部件 ID、校验修改、保存不可变版本？ |
| 7 | `frontend/src/components/furniture/DeterministicFurnitureModel3D.tsx` | 结构化几何如何变成 Three.js 模型？ |
| 8 | `backend/app/workers/`、`backend/app/services/model_call_governance_service.py` | 异步任务、租约、重试和成本如何治理？ |
| 9 | `tests/integration/test_agent_chat_facts.py`、`test_design_agent_api.py` | 如何用测试证明多轮修改、幂等和权限边界？ |

## 验收命令

从仓库根目录执行，Python 使用已安装后端依赖的解释器：

```powershell
python -m pytest -q --basetemp=outputs/pytest-local-v1
ruff check backend/app backend/evals tests
python tests/development_live_acceptance.py --qa --qa-facts-only --output outputs/development-qa-local.json
```

前端在 `frontend` 中执行 `npm test` 和 `npm run build`。最后一条 Python 命令需要运行中的开发 API，会产生真实模型调用；它保存原始问答，不自行宣称全部验收通过。

浏览器脚本 `tests/development_browser_acceptance.cjs` 使用本机 Chrome、Playwright 和 sharp；需要在运行环境中提供这两个测试工具包，不属于应用依赖。执行 `node tests/development_browser_acceptance.cjs` 会创建开发任务、调用模型并输出桌面／手机截图与报告到 `outputs/v1-browser/`，不会输出会话凭据。

## 保留的边界

- 局部三维编辑与图像效果生成不是同一种能力；生成效果图不能证明场景几何可制造。
- 当前事实证据检查能约束字段、类型、原文片段和尺寸／预算关系，不能证明模型在所有语言表达下都理解正确。
- 模型服务异常或提取失败保留已有需求；不要通过填默认尺寸或模板假成功绕过失败。
- 开发模型成本可能使用仿真单价，不等于供应商账单。
- 未登录时 `/api/auth/me` 返回 401 是登录态探测，不是匿名设计任务失败。
- 本地运行报告、截图、数据库备份和 `.env` 不作为代码提交内容。真实业务上线前需另行完成数据授权和生产评测。

历史记录见 `PROJECT_STATUS.md`；开发数据规则见 `开发版数据与验收说明.md`；原始阶段验收边界见 `阶段1至4验收证据核对.md`。
