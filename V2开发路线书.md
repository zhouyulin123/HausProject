# V2 开发路线书

更新日期：2026-09-20。状态：**V2 开发版功能闭环完成**。整屋空间、通用编辑、冻结家具、变更影响、安装/预留/点位、跨领域受控 AI、商品与材料估价、清单比较、冻结交付、审阅和受控分享已接通并完成联合开发验收。现有物品与规则属于开发代表案例；真实授权住宅、商业审核、专业评审、受保护发布和生产统计性能仍是正式上线门禁，因此不称“完整 V2 产品级验收通过”或可施工/可采购。

V1 的已实现能力、启动方法和验收边界统一见 [V1 项目阅读指南](./V1项目阅读指南.md)。本文维护 V2 的产品目标、能力全貌与执行路线。第 9 节记录全权施工授权后的默认决策与估算；其他方向保留为产品全貌，不自动扩大交付承诺。

## 1. 讨论前提

用户提出的大目标是映射出 3D 户型，并在其上提供家装设计服务。衣柜设计、厨房设计、家具摆放是说明方向的例子，不是完整需求清单，也不代表优先级或实施顺序。

因此，一级规划要先回答“用户可以在系统中把自己的家设计到什么程度”，再讨论支撑这些目标的通用能力和首批场景。不能把举例直接拆成三个开发阶段，也不能把下文能力地图中的所有方向都默认纳入 V2 承诺。

## 2. V2 的产品目标

以真实住宅空间为共同基础，让用户表达居住需求，建立和修正户型，设计空间与各类家装要素，比较方案的效果、使用条件和预算，并持续修改直至形成可沟通、可复核的整屋方案。

3D 户型承担空间载体的作用。用户修改某个设计要素时，相关尺寸、使用空间、材料、清单和预算应当能够关联更新，而不是分别生成互不对应的结果。

拟讨论的用户主流程：

```mermaid
flowchart LR
    A[住宅资料与生活需求] --> B[空间重建与事实确认]
    B --> C[整屋设计与多领域协同]
    C --> D[效果、功能与预算比较]
    D --> E[方案确认与交付]
    D --> C
```

## 3. 家装能力地图

以下按用户需要解决的问题梳理能力，不是封闭清单。示例用于说明能力覆盖面，执行深度按第 9 节基线细化。

| 能力领域 | 用户要解决的问题 | 场景示例，非功能边界 |
| --- | --- | --- |
| 居住需求与功能策划 | 谁居住、怎样生活、现在有哪些问题、哪些需求最重要 | 家庭成员、生活习惯、居家办公、收纳、适老、预算分配 |
| 户型与建筑空间 | 空间是什么样、尺寸与连接是否准确、哪些条件不可随意改变 | 房间、墙体、门窗、层高、梁柱、固定障碍、多房间关系 |
| 空间组织与使用体验 | 每个区域做什么，活动是否顺畅，功能能否共存 | 功能分区、动线、开放区域、家具布局、活动和开启空间 |
| 硬装与表面设计 | 墙、地、顶以及固定装饰如何组织和表达 | 地面铺装、墙面材料、吊顶、背景墙、门套、局部造型 |
| 家具、定制与收纳 | 物件如何适应空间、使用需求和整体设计 | 成品家具、定制柜体、储物系统、组合家具、局部构件 |
| 灯光、设备与点位协同 | 照明与设备怎样配合空间、家具和日常使用 | 照明层次、灯具、家电、已有插座与开关、空调与其他设备占位 |
| 软装与整体风格 | 各种颜色、材质和物件组合后是否协调 | 窗帘、地毯、饰品、配色、材质搭配、全屋风格 |
| 专项空间综合设计 | 一个空间内的多类要素如何一起工作 | 厨房、卫浴、阳台、玄关、卧室、客餐厅、书房等 |
| 材料、预算与交付 | 方案需要什么、成本如何组成、如何交给后续人员 | 材料与产品选择、工程量、分项估价、方案比较、清单与交付资料 |

专项空间是上述能力的组合。例如厨房涉及空间、表面、柜体、设备和使用约束；衣柜涉及收纳、家具结构、墙面适配和开启空间。未来新增场景应尽量复用这些能力，而不是重复建设一套独立数据与交互流程。

设备点位表达、设计建议、专业工程计算和可施工结论是不同深度。能力地图展示研究范围，不表示系统已经具备专业设计或施工审核能力。

## 4. V1 基础与 V2 的共性缺口

| V1 可复用基础 | 需要补足的共性能力 |
| --- | --- |
| 需求对话、事实证据、确认和恢复 | 从单次需求扩展到家庭目标、空间目标与跨领域约束，并能处理相互冲突的要求 |
| RoomModel 多房间感知结果 | 统一整屋尺度、坐标、共享墙体、门洞及房间连接，保持原图证据与人工修正 |
| 单房间 3D 和场景编辑 | 整屋／房间／对象多层级编辑，2D 与 3D 同源，空间和设计要素关联更新 |
| 柜体、桌类及开放几何 | 可持续扩展的设计对象表达，涵盖构件、表面、设备等不同对象的参数与关联 |
| 边界、碰撞及部分门洞净空校验 | 按使用场景增加活动空间、动线、安装和设备使用等可复算约束 |
| 商品、确定性报价与版本化交付 | 多类材料和设计要素的计量、成本归属、整屋预算及交付一致性 |
| Agent、受控工具、Worker 与成本账本 | 在共同空间上下文中协调多项设计任务，处理变更影响与局部重算 |

已核对的空间缺口：当前房间转换会分别居中缩放，不能直接拼成保持原位置关系的整屋；固定障碍只记录名称、房间和置信度，尚无精确避让所需几何；原图持久化和校准到正式设计的完整衔接也仍需补齐。

这些是已有代码暴露的共性问题，不意味着 V2 只能围绕家具展开。

## 5. 一级路线：按能力依赖推进

以下按能力依赖组织工程顺序，具体工作包由 Codex 在实施时细化；初步时间预算见第 9 节。真实资料与用户／设计师评审从范围细化时开始准备。

| 阶段 | 核心问题 | 主要成果 | 验收关注点 |
| --- | --- | --- | --- |
| A：产品范围与设计深度 | 服务谁，帮助用户完成哪些决定，结果交给谁 | 用户流程、能力地图、首批场景选择、交付定义和可评审样例 | 区分愿景、V2 承诺和后续方向；明确每项能力是展示、编辑、建议还是专业深化 |
| B：可信的整屋空间底座 | 用户的住宅能否准确表达并持续修正 | 资料与原图持久化、尺度和结构确认、统一整屋模型、2D／3D 联动、多层级查看与恢复 | 多房间位置与连接一致；未知事实可见；错误识别能修正；修改后可恢复 |
| C：通用家装设计能力 | 不同家装要素能否在同一空间中设计 | 设计对象与表面表达、放置／适配／编辑、材料属性、关联关系、约束与版本管理 | 几何、参数和清单一致；能表达不同种类要素；修改能追踪影响 |
| D：代表性场景闭环与协同 | 通用能力能否解决实际家装问题 | 从能力地图选择代表场景，打通需求、设计、校验、比较和修改；逐步加入领域规则 | 场景有完整用户价值；跨领域修改不丢关联；可复用能力得到验证 |
| E：整屋方案整合与交付 | 用户能否理解取舍并确认一套一致方案 | 功能与视觉方案比较、材料和产品清单、整屋预算、版本确认、分享与交付 | 场景、效果、清单和预算引用一致；缺口和待报价项明确 |
| F：真实试点与扩展 | 哪些能力可稳定使用，下一步扩展什么 | 真实案例评测、评审反馈、失败修复、性能与体验完善 | 根据真实问题验收；按用户价值和共性复用程度扩展场景 |

A 明确范围后，B 与 C 应通过小型端到端场景逐步验证；D 的场景设计与 E 的交付设计可以提前介入。不能先堆一个庞大的通用系统，直到最后才检验是否解决用户问题。

### 如何选第一个可体验版本

先定义用户需要完成的一项完整设计任务，再选足以支撑它的空间输入、编辑能力和交付形式。首批任务需要同时满足：

- 有明确的目标用户和可判断的成果；
- 能验证整屋空间与实际设计之间的关联；
- 具有代表性，后续领域可以复用其主要能力；
- 所需数据、模型、规则与评审条件能够准备；
- 范围可控，成功和失败都有明确判据。

不因用户举例而预先指定衣柜、厨房或家具布局为首批专项。全权施工授权后的首个闭环与输入范围见第 9 节，后续根据共性依赖和验证结果调整。

## 6. 能力深度与范围控制

同一个功能名称可能对应不同工作量，规划时必须明确做到哪一层：

| 深度 | 用户获得的能力 | 需要的基础 |
| --- | --- | --- |
| 表达与查看 | 在空间中看见设计内容 | 几何、材料、位置与信息表达 |
| 参数化编辑 | 修改尺寸、构成、材料和关系 | 稳定对象身份、编辑规则与版本恢复 |
| 智能建议与校验 | 根据需求得到方案并理解冲突与取舍 | 受控工具、领域约束、可验证结果与失败处理 |
| 专业深化与实施交付 | 形成支持采购、制造或施工的可靠资料 | 专业数据、规则来源、计量与审核责任、对应真实验收 |

V2 可以让不同领域达到不同深度。不能因为能显示某件物品，就认为已经完成它的设计服务；也不能因愿景包括施工交付，就默认本期必须覆盖全部专业工程。

既有权限、成本、版本和安全门禁继续有效。专业深化不默认纳入当前执行基线，不能由举例自动推导。

## 7. 继承 V1 的技术方向

本节仅列共性约束；具体 schema、接口、工具与库选型在范围确定后细化。

1. 复用现有前后端、Three.js、LangGraph、Worker、商品与报价服务；按实际场景需要扩展，不预先建立复杂插件平台。
2. 建立统一米制的整屋坐标与稳定的空间对象身份，明确全局和局部坐标关系；兼容 V1 单房间历史版本。
3. 原始资料、识别结果、人工确认和设计修改保留来源；2D、3D 与交付从共同事实生成，效果图不自动成为结构事实。
4. 设计要素共同具备身份、位置、参数、来源和版本；表面、构件、家具、设备等保留各自必要的领域语义，不强行都当作商品家具。
5. 几何、配置、约束和清单建立明确关联；不同单位在固定边界转换，不能在各页面自行猜测。
6. 修改空间或设计对象后识别受影响的领域、清单、报价与渲染，进行局部重算和校验；旧交付保留原版本。
7. AI 负责理解、建议与受控规划；确定性工具执行和验证，跨领域动作沿用并发、幂等和失败回滚机制。
8. 缺少尺寸、规则或价格时明确保留缺口，不以默认值、模板或手写结果制造成功。

## 8. 验收与证据

| 层级 | 验收内容 | 证据边界 |
| --- | --- | --- |
| 工程正确性 | 坐标、单位、关系、约束、引用、版本、恢复、权限和失败状态 | 自动化测试不能替代真实设计质量 |
| 产品可用性 | 用户完成设计任务的程度、人工修正成本、方案可理解性、端到端编辑和交付、桌面与移动体验 | 截图或模拟响应不能证明真实识别准确性 |
| 领域设计质量 | 获授权住宅资料与实测／人工标注对比，各已承诺领域的专业评审和失败复测 | 不同领域分别评价，不能用一个成功样例证明整体可用 |

范围明确后再冻结指标：空间精度与完整性、对象与关系一致性、约束通过情况、完成任务所需人工干预、修改影响的正确性、成本、响应与恢复表现。具体阈值根据输入条件、领域要求和试测基线确定，不提前编造达成数字。

复用 V1 真实案例治理与受保护评测；保留至少 20 个合格真实案例及 development／regression／blind 三组切分的既有最低门槛。新增领域还需相应样本覆盖，达到最低案例数不代表覆盖所有家装能力。

## 9. 全权施工执行基线与工期估算

### 授权与默认决策

用户已授权 Codex 负责施工，未明确的常规产品和技术选择由 Codex 判断并记录，不再将每项待定选择作为等待用户确认的阻塞。

- 目标用户以业主自行设计、设计师可复核为主；交付完整的住宅设计辅助产品。
- 首批空间范围采用单层住宅，支持多房间及常见直线墙体的非矩形轮廓；先用户型图、人工描绘与实测尺寸建立可修正空间，照片提供补充证据。
- 空间、硬装表面、家具与收纳、软装应具备真实编辑、持久化和版本关联；灯光、设备及已有点位具备空间表达、参数编辑与明确适用范围的约束。
- 智能建议覆盖需求、空间利用、材料风格、配置与预算，但按具体受支持操作逐项验收；功能名称存在不算完成。
- 专项空间以共性能力组合实现，逐步覆盖普通住宅主要空间；不以用户举的几个例子限定工程，也不承诺穷尽每一种布局和工艺。
- 最终整屋方案包含可恢复的空间和设计版本、方案比较、各房间视图、材料／产品清单及确定性分项估价；缺少商业数据的项目保留待报价，不编造价格。
- 自动施工图、水电气专业计算、结构判断、制造拆单和无人工校准的扫描重建，不计入本次工期基线；若后续纳入则重新评估。

首个可体验闭环采用“住宅资料输入 → 多房间校准与修正 → 2D／3D 编辑 → 同一空间中的表面与物件设计 → 保存恢复与查看修改结果”，用于验证空间和不同类型设计要素的共同基础。它是中间交付，不代表完整 V2。

### 时间预算

初步完整工期为 **12–20 周**，首个可体验闭环约 **3–5 周**。这是基于现有代码缺口的项目工作预算，假设持续投入、运行环境可用、首批数据与评审能够随开发提供；不是已测得的 Codex 执行速度，也不是无条件自然日交付承诺。

| 工作包 | 初步预算 | 包含的验证 |
| --- | --- | --- |
| 执行规格、空间契约与风险验证 | 1 周 | 既有实现核对、数据迁移与兼容策略、代表性测试条件 |
| 整屋建模与统一编辑 | 2–4 周 | 坐标与拓扑、校准、编辑联动、保存恢复、历史兼容 |
| 多领域家装设计与受控智能操作 | 3–4 周 | 表面、构件、物件、材料、设备表达及领域约束；功能与失败回归 |
| 跨领域协同、方案比较与整屋交付 | 2–4 周 | 修改影响、预算和清单一致性、渲染绑定、分享与交付 |
| 集中联调、真实样例试用与返修 | 2–3 周 | 前后端全量回归、浏览器与 3D 验证、权限与并发、性能、领域评审 |

工作包合计 10–16 周，再预留约 20%–25% 的不确定性余量，形成 12–20 周总体预算。测试贯穿每个工作包，最后 2–3 周用于集中联调和问题收敛，不是最后才开始写测试。预计验证、联调和返修占总体投入约三分之一，已计入估算。

### 估算依据与调整方式

- 可复用 V1 的任务与权限、Agent、成本治理、Worker、版本和报价基础，减少从零建设成本。
- 主要新增复杂度在整屋拓扑与交互编辑、设计要素语义、跨领域变更影响和真实领域约束；不能通过增加页面或几段提示词代替。
- 当前没有 V2 实际交付吞吐记录，也没有本轮全量测试运行耗时测量；历史测试通过数量只说明已有回归基础，不用于推算实现速度。
- 完成空间契约与首个端到端闭环后，用实际完成的验收项、返修量和剩余风险重估。后续每个里程碑更新剩余工作，不机械沿用最初区间。
- 真实授权资料、商业价格与专业评审等待时间无法由编码速度消除。暂缺时可继续工程实现和明确标识的开发验证，正式业务验收另行保持未完成；外部等待不计入上述有效工作预算。
- 全权施工授权用于推进项目，不表示无人运行任务时会自动持续执行；不能将工作预算直接解释为全天候后台倒计时。

后续进展与估算调整继续维护本文，不新增并列计划书。

## 10. 主任务管理与架构契约

### 分工与验收责任

- 主任务拥有架构决策、跨模块接口冻结、依赖与进度管理、代码整合及最终验收责任；子任务提交的“通过”需要主任务复核证据。
- 子任务按边界拆分：后端空间与持久化、前端交互与视觉、验证与独立审查。每次明确允许编辑的文件、输入输出、禁止事项和验收要求，避免同时改同一模块。
- 界面由主任务自主决策并下达返修：采用以空间画布为主体的设计工具，按整屋／房间／对象层级组织，工具与属性面板按需展开；保持可操作、可恢复、可理解的状态。
- 产品验收同时检查正常流程、失败恢复、无数据状态、权限、并发和幂等；前端用 Playwright 检查桌面与移动视口、文字溢出、图片加载、3D 像素非空与操作变化。模拟响应只证明前端行为，不证明实际识别效果。
- 日常常规决策自主推进；缺少真实授权数据等外部条件时继续不依赖该条件的工作，并明确保留正式验收缺口。

### ADR-01：整屋与单房间契约分离

- RoomModel 保持感知与校准事实角色；SceneDocument 1.0／1.1 保持既有单房间版本语义，不原地覆写历史方案。
- 新整屋文档归属于 DesignTask，先于商品方案存在；使用统一米制全局坐标与稳定 room／wall／opening 身份，明确相邻关系和来源证据。
- 现有 room_model_to_scene 按各房间包围盒缩放并居中，不可循环调用后直接拼成整屋；原始识别缺少统一坐标和尺度证据时不能自动声称整屋已校准。
- 整屋空间采用主记录与不可变版本，场景投影绑定精确空间版本和房间身份；整屋编辑不能静默改写已经确认的单房间交付。
- GET／PUT /api/design/tasks/{task_id}/space 已实现并通过本批隔离工程验收：任务归属校验；写入携带 base_version、client_mutation_id 和 document。父任务加锁后先检查幂等，再检查版本；同键不同内容与旧版本返回 409，非法几何返回 422。
- 任意房间支持凹多边形时，不能直接套用现有仅适用于凸多边形的 SAT 重叠检测；几何依赖在选型与边界测试后确定。

### 首批接口：空间来源恢复

GET /api/design/tasks/{task_id}/agent-state 增加可空 room_source，字段为 image_id、image_url、file_name。它与 room_model 必须取自同一条通过 RoomModel 校验且属于当前任务的 UploadedImage；无有效模型则二者为空。

当前沿用“最新追加有效图片”的服务端选择规则。新上传若识别失败，保留原有效上下文；旧上传幂等重放若返回较小图片 ID，保留较新上下文并提示。该规则不是可编辑空间版本控制的替代品；同图片后续校准的并发版本仍需下一阶段专门处理。

前端将模型与来源成对恢复，缓存升级为 v6；原图不可用时显式提示。当前预览通过 image_id 请求鉴权内容接口，用临时 Blob 保留完整图片比例，不在 URL 中放会话标识。已登记 UploadedImage 的旧静态地址拒绝公开读取；公共商品图继续公开。派生效果图等其他资产仍需独立访问评审，不能将原图保护等同于全部资产私有化。

### 第二批接口：整屋空间版本

- 文档采用 spatial/1.0，字段统一 snake_case：unit 固定 m，scale_status 为 unconfirmed／confirmed，source_image_id 可空；rooms、walls、openings 分别保存房间净轮廓、显式墙体和墙体开口。坐标约定为右手系 Y 向上，房间多边形在全局 XZ 平面，不独立居中缩放。
- 房间包含 id、name、polygon、height；墙体包含 id、start、end、room_ids、height、thickness；开口包含 id、wall_id、type、offset、width、height、sill_height。开口偏移从墙体 start 起算，共享墙使用同一墙体 ID。
- GET／PUT 响应为 task_id、version、document；尚未保存时 version=0、document=null。PUT 接收 base_version、client_mutation_id、document。相同请求重试返回该次历史版本，GET 返回最新版本；同键不同请求不能覆盖原记录。
- 支持简单凹多边形，拒绝自交、面积重叠、悬空引用、重复墙段和超出墙体的开口；允许房间边界接触。墙体和开口允许为空，表示尚无这些明确事实，不自动补齐结构。source_image_id 只允许绑定当前任务图片，图片引用本身不证明尺度已经校准。
- 后端两张新增表为 design_spaces、design_space_versions；迁移 a1b2c3d4e5f6 接实际代码链尾 e5a6b7c8d9e0。保留原有 RoomModel 和 SceneDocument 数据，不自动迁移旧房间。本机开发库已按备份流程升级，详见最新维护记录。
- 可选 image_reference 保存底图在全局 XZ 的 origin、width、depth，必须绑定当前任务 source_image_id；统一缩放同步调整底图与水平几何，垂直尺寸保持不变，尺度仍需明确确认。
- 历史接口为 GET /space/versions（游标分页）、/space/versions/{version}、/space/versions/{version}/source；草稿按 /space/sources/{image_id} 在当前任务权限下解析来源，恢复旧版不误用最新图片。
- GET /space/versions/{version}/rooms/{room_id}/scene 只投影精确版本、已确认尺度且能被旧 SceneDocument 无损表达的房间。墙体缺失、半高墙、非统一墙厚或开口跨轮廓分段等不能精确表达的情况返回 422；不补造墙体，不更新历史方案。

### 当前施工清单

| 工作包 | 状态 | 交付与下一步 |
| --- | --- | --- |
| V2-A01：现状与架构审查 | 开发版完成 | 能力地图、适用深度、ADR 与正式验收边界已冻结；专业利益相关者评审待外部 |
| V2-B01：空间原图与模型恢复 | 开发版完成 | 服务端来源绑定、前端缓存恢复、原图私有访问、失败保留和乱序保护 |
| V2-B02：任务级整屋契约与持久化 | 已通过工程验收 | schema、版本表、归属校验、历史、幂等与并发；开发库备份升级及真实 MySQL 并发验证完成 |
| V2-B03：整屋编辑与单房间投影 | 已通过编辑闭环验收 | 底图描绘、尺度确认、全局房间／共享墙／门窗编辑、2D／3D、草稿和历史恢复、精确投影；真实空间精度仍需授权实测案例验收 |
| V2-C01：通用概念编辑 | 开发版完成 | 五类物件、表面、材料、2D/3D、版本与确定性几何检查；非专业领域深化 |
| V2-C02：家具冻结入屋 | 开发版完成 | 商品确定性模型与同任务开放几何冻结为私有资产，精确版本恢复；非采购或制造认证 |
| V2-C03：空间变更影响 | 开发版完成 | 只读影响预览、显式引用修复、跨空间撤销/恢复，旧交付不可变 |
| V2-C04：安装/预留/点位 | 开发版完成 | 显式安装、五向使用预留、已有点位与关联检查；非承重、水电容量或施工安全结论 |
| V2-D01/D02：受控 AI 建议 | 开发版完成 | 幂等候选、人工确认、受控资产、确定性预算及跨领域真实模型四轮证据；不替代设计师审核 |
| V2-E01：清单与比较 | 开发版完成 | 精确版本、面积/件数、JSON 下载及版本差异 |
| V2-E02：商品与材料估价 | 开发版完成 | 商业商品门禁、显式材料规则、费用证据冻结、未知项待报价；开发材料规则非市场审核价 |
| V2-E03：冻结交付/审阅/分享 | 开发版完成 | 不可变交付、打印视图、追加审阅、白名单分享、到期与撤销；非施工交付/PDF 专业审图 |
| V2-F：开发性能基线 | 有限通过 | 桌面 200 件、手机 80 件单次冒烟；不代表 p95、长会话内存或生产 SLA |
| V2-F：正式试点与质量飞轮 | 外部门禁未完成 | 机制已具备；仍需授权案例、三分组、商业与专业评审、受保护发布和生产环境证据 |

## 11. 文档维护记录

### 2026-09-20：V2 开发版功能闭环最终验收

- 用现有商品、开放几何家具与材料规则作为开发代表案例，完成单任务联合旅程：已确认 6m×5m 客厅 → 30㎡地面规则 → AI 使用经授权商业家具 → 用户确认并保存家装 V3 → 确定性总价 4,280 元 → 冻结交付 → 受控分享。`tests/v2_representative_journey_browser.cjs` 在 1440px/390px 隔离真实 API 通过，公开页引用同一版本且无横向溢出；证据位于 `outputs/v2-representative-journey/`。
- 真实 `gpt-5.6-terra` 在临时 SQLite 完成四轮跨领域连续修改：加入授权休闲椅、增加未绑定报价的浅色橡木地面、保持家具/饰面并移动家具、拒绝承重墙拆除和安全承诺。四次均真实计费，共 34,355 tokens、0.03483 CNY，单轮约 3.0–6.9 秒；报告为 `outputs/v2-home-agent-d02-live/report.json`。模型只生成白名单操作，价格仍由服务端复算。
- Home Agent 现在只把归一化的已确认需求事实加入上下文，并继续传递精确空间、当前设计、最近六轮、当前/允许资产和预算。未知需求字段不进入模型上下文，AI 候选仍需用户确认后保存。
- 材料规则由用户显式绑定，不按材料名猜价；冻结请求量、计费量、损耗、最低量、安装/运输/税、数据版本和记录版本。30㎡验收规则得到 3,600 元；再增加未绑定顶面后已知小计保留而总价回到待报价。规则更新/停用使用 `expected_record_version`、行锁和版本递增；真实 MySQL 并发用例只允许一个同版本写入成功。
- 独立安全与产品审查后修复：私有空间/家装 GET 的 `no-store` 与 `Vary: X-Session-ID`、公开分享移除内部 `quote_rule_id`、核心交付与辅助历史隔离失败、一次性链接与分享 ID 绑定、规则读取失败保留原绑定、估价费用证据可解释展示。未发现 P0。
- 大场景资源策略在桌面 200 物件和手机 80 物件单机单次通过：2D 不预取资产、重复冻结资产只取一次、3D 非空并可旋转；该报告不是 p95、生产网络、内存或长会话 SLA。
- 二次独立复审后最终全量回归：后端 `1634 passed, 8 skipped`（495.14 秒），仅保留已知的 Python 3.14 / Pydantic v1 兼容警告；前端 93 个测试文件共 485 项通过，TypeScript 与 Vite 7 生产构建通过，构建仅保留既有 `vendor-three` 约 667 KB 分包提示。材料规则真实 MySQL 并发专项 1 项通过；相关 Ruff、`npm audit`（0 漏洞）与 `git diff --check` 通过。
- 二次复审补齐五类健全性问题：V2 空间/Agent/校验/估价/交付变更现纳入受保护真实案例 proof；私有接口统一 `no-store` 与 `Vary: X-Session-ID`，两类分享令牌路径日志脱敏，公开数据库异常返回脱敏 503，交付分享凭证碰撞锁内有限重试；清单与版本历史独立加载，辅助失败不再遮蔽有效结果。Vite、React 插件、Vitest 和 Playwright 使用受支持版本，Node 契约收紧为 `>=20.19 <25`。
- GitHub Actions 新增不依赖业务数据库或外部模型的 V2 代表旅程：隔离 SQLite API、受控 Agent、Vite 和项目内 Chromium 完成 1440px/390px 空间、材料、AI、估价、冻结交付和分享；失败上传截图与服务日志。本机按同一命令复跑通过。真实付费模型和受保护住宅案例仍只在对应受控环境运行。
- 统一启动器已在本机开发库完成 Alembic 就绪检查并拉起 API、Generation、Effect Render、Blender 与 Frontend 五个进程；`http://127.0.0.1:8081/ready` 返回数据库、Schema、存储等检查正常，开发体验入口为 `http://127.0.0.1:8080`。
- 正式上线门禁保持未完成：至少 20 个获授权并标注的 `private_real` 住宅及 development/regression/blind 分组、真实商业商品和材料价格审核、设计/施工专业评审、受保护 runner/签名/required check、生产网关分享 URL 脱敏、部署回滚和统计性能基线。开发案例不得改称正式真实案例，系统输出不得称可施工、可采购或专业认证。
- 2026-09-16 的 12–20 周为范围未知时的历史初估，不再作为剩余工期。当前可停止扩功能；后续应转入正式数据、评审、部署和质量飞轮，而不是继续增加平行页面。V2 首轮完整闭环已提交并推送为 `f67d060`，二次复审改动也已按阶段统一提交。

### 2026-09-18：继续完整 V2，启动空间变更影响闭环

- 用户要求继续完整 V2，主任务沿用第 9 节执行基线，不缩减为家具单领域，也不把工程测试等同真实业务验收。Codex 子任务分开承担影响分析后端、前端更新/恢复和剩余范围独立审查；不使用 Gemini，不提交 Git。
- 本批新增只读 `POST /api/design/tasks/{task_id}/home-design/space-impact`，输入当前草稿 document 与精确 target_space_version。服务端读取同任务原/目标空间与冻结家具，返回空间差异、失效引用、候选文档和几何复核；不持久化、不挪动、不删除家具或饰面。
- 前端显式预览、处理失效房间/墙体引用，再切换到目标空间草稿；仍需保存形成家装新版本。尺寸不自动缩放，几何冲突保留为待修正问题，未确认尺度不能切换。撤销/重做必须同时恢复设计与精确空间，旧交付不变。
- 主任务独立冻结资产/历史/失效引用 API 用例已先运行，因新接口尚不存在而 RED；后续需后端专项、前端恢复测试和桌面/手机真实 API 浏览器验收转绿，才能记为通过。
- 后续继续安装与使用约束、可追溯分项估价、可读交付与受控分享、真实性和性能验收；正式案例授权、商业核验与专业评审保持独立未完成状态。
- C03 独立 API 2 项、后端专项及既有 API 共 33 项通过；前端子任务全量 445 项和构建通过。主任务 `tests/home_impact_browser.cjs` 在 1440/390 真实隔离 API 验证越界预览、不自动保存、跨空间撤销/重做、刷新恢复、重新归属、历史交付不变和 3D 像素/旋转，已检查截图。鉴权复用现有会话活跃时间维护，所谓只读指不修改空间、家装、资产或历史业务记录，不另造鉴权逻辑。
- 独立审查发现商品冻结准入只做结构验证，损坏位置/几何/缩放/包围尺寸可能被接受。已追加服务端原参数重编译全规则比对，五类 RED 转绿；已冻结历史不重写。仍继承编译器概念级包围约定，不宣称精确网格碰撞；性能预算和重复选择消耗配额继续处理。

### C04 本轮接口决定（已完成）

- 以可选字段扩展既有 `home-design/1.0`，未提供的新字段序列化时省略，保证旧请求摘要与历史兼容；不通过名称推断安装方式或设备要求。
- 物件 `installation` 可空，非空为 `{kind: floor|wall|ceiling, wall_id?: string|null}`。沿用全局位置与米制；落地底面为 0、吊装顶部与房间顶面接触、墙装必须明确本房间墙体并满足背面接触。落地冻结资产仍不能改成墙装或吊装。
- 物件 `clearance` 可空，非空为 `{front, back, left, right, above, confirmed}`，距离 0–10 米且必须显式输入，方向以物件本地轴解释。用户确认的是使用要求，不是专业认证；未确认不假称检查通过。
- 文档 `points` 可选，点位包括 `id/name/room_id/kind/position/confirmed`；kind 为 socket/switch/water/drain/network/other，表示已有点位记录，不自动设计管线。物件可选 `point_requirement: {point_id, max_distance_m}`，仅复算显式关联的同房间点位距离与确认状态。
- 安装、预留空间和点位检查作为确定性问题返回；不存在宿主/点位归属为引用问题。AI 不得把未确认条件变成 confirmed，新的事实需用户明确操作。完整交付保留适用范围，不输出承重、电气容量或施工安全结论。

### 2026-09-18：C04 安装、预留与现场点位接通

- Codex 后端和前端子任务完成可选安装约束、五方向使用预留、已有点位录入与物件关联；沿用同一版本、撤销和草稿恢复，不新建平行编辑页面。主任务遵循管理、TDD 和验证技能独立复核，不提交 Git。
- 后端子任务安装及既有家装/影响/Agent 回归 102 项、新增持久化 1 项通过；主任务独立发现冻结落地家具可被改成吊装，新增 RED 后补齐绑定门禁，资产/安装/API 26 项通过。点位跨房间或缺失现在返回结构化影响问题，候选可显式解除或重新关联，不自动删除物件。
- `tests/home_installation_browser.cjs` 使用隔离 SQLite 真实 API，在 1440/390 验证点位默认未确认、人工确认、撤销/重做、刷新恢复、安装和预留编辑、关联点位、保存与确定性检查；3D 非空且旋转改变像素，无横向溢出和页面异常。报告与截图位于 `outputs/v2-home-installation/`。没有以测试桩声称真实模型验收。
- 主任务为家具库同来源版本生成稳定幂等键，避免重复选择持续新建快照；资产请求最多并发 4 个，已完成缓存保留最近 32 项。此限制不是整页 WebGL 内存上限，活动场景、完整交付响应及大方案实机性能仍需继续验收。
- 当前前端全量 85 文件 456 项通过；本批后端集中回归 57 项、TypeScript 和生产构建通过，保留既有 Three.js 包体提示。可追溯估价、可读交付和分享、跨领域真实 AI、多场景产品验收及正式授权案例仍未完成，不能标记完整 V2 产品交付。

### 2026-09-18：E02/E03 估价与交付契约（已完成）

- E02 新增独立估价快照，创建绑定精确 home_version、地区、幂等键，服务端读取对应冻结商品与当前商业资格，不接受客户端金额。商品版本变化、未核验、地区/库存/价格有效期不符、区间价未明确选配或没有商业绑定的条目为 null/待报价；已知小计不冒充整屋总价。读取与重放不重算已冻结价格。迁移 e5f6a7b8c9d0 已编写，业务数据库尚未升级。
- E03 冻结交付独立于每次重算的概念清单，绑定原始清单摘要和可选估价；确认记录追加且不代表专业认证。私有交付只保存模型资产 ID/摘要而不复制所有大模型，精细预览继续使用原任务鉴权。
- 分享使用独立白名单投影：用户必须明确授权暴露户型、尺寸及自填清单；首版不公开原图、原图引用、私有精细模型、资产来源路径。只保存随机 token 哈希，明文仅首次返回；同键重放只返回已有分享记录，所有者可按 ID 撤销，链接过期/撤销统一不可用。撤销不能收回接收者已下载的内容。
- 后端审查、估价前端和交付后端分开施工，主任务负责接口衔接与独立浏览器验收。当前为施工记录，不表示 E02/E03 或完整 V2 已验收。

### 2026-09-18：E02/E03 首版联合验收与开发库更新

- E02 前端区分已知小计、待报价与当前清单合计，历史绑定精确家装版本和地区；同键重试保留原请求。E03 接通冻结交付、逐房二维图与清单、私有精细预览、浏览器打印视图、追加审阅、明确授权分享、期限与撤销。公开页不加载原图、私有模型或其资产 ID。
- 通过隔离真实 API 的 1440/390 像素 Playwright 验收：估价响应丢失后刷新重试、较早估价分页选择、冻结、审阅、409 后解除本机重试记录、分享响应丢失/同键重放不返回令牌、公开投影、无会话请求、撤销后 404、3D 非空与旋转、无横向溢出。打印样式与截图已检查，不等同正式 PDF 分页或专业交付验收。证据目录为 outputs/v2-home-quotes 与 outputs/v2-home-delivery。
- 独立审查发现并修正：永久冲突锁住表单、渲染错误未连到重试、分享授权漏列材料/估价、只能选择最近十条估价。主任务另修正独立页画布尺寸、公开页切换令牌状态、数据库无时区时间响应，以及正式应用中间件覆盖 Referrer 策略/日志记录分享令牌的问题。新服务使用统一 app.run_api 入口，不开 Uvicorn 原始访问日志；外部网关仍须配置分享 URL 脱敏。
- 前端最终全量 88 文件 469 项通过、TypeScript/生产构建通过；估价与交付后端专项 36 项、时间修复回归 17 项通过。后端全量 1585 项通过、5 项跳过（571 秒）；全量启动后补充的中间件隐私改动另外跑 9 项通过。保留 Python 3.14/Pydantic V1 兼容提示与既有 Three.js 分包提示，不将这些提示写成已解决。
- 最终补充模型渲染故障注入，暴露成功下载但渲染失败的资产仍留在缓存、重试反复命中坏模型的问题。已增加完成缓存失效与 hook 接线，单测先失败后通过；桌面与手机均验证异常提示、重新取回服务端模型、恢复显示，并重新跑通估价/审阅/分享/撤销。只允许本次注入的明确渲染错误，后续流程没有额外页面异常。
- 开发库升级前备份：outputs/backups/houseproject_pre_f6a7b8c9d0e1_20260918_105337.sql，mysqldump 成功，4,946,551 字节。已从 d4e5f6a7b8c9 经 e5f6a7b8c9d0 升至 f6a7b8c9d0e1。新版 8084 已启动且 /ready 通过，8085 为体验前端；未替换既有 8081。没有提交或推送 Git。
- 下一批仍需：D 的真实模型多轮跨领域设计及受控商品使用、材料规则与预算归属、大场景资源性能、MySQL 新交付并发专项、专业与真实案例评审。商品待报价不补伪造价格；合成工程夹具不能记作获授权真实案例。完整 V2 尚未完成。

### 2026-09-17：C02 首版集成与独立验收

- Codex 后端、前端分别实现冻结资产与家具库；主任务完成架构审查、独立 API 测试、真实 API 浏览器和迁移核对，没有提交 Git。独立测试查出未知 renderer 被标记可用，返工增加与前端一致的渲染器白名单后转绿。
- 新建任务私有 HomeDesignAsset，冻结商品 record_version/SKU/data_origin/data_version/verification_status，或当前任务开放几何精确版本。客户端不可提交模型快照；同键重放先于来源版本检查，来源停用或更新不改变旧快照。单任务最多 200 快照、单模型 1 MB/256 部件，不接受缺失部件、材质、未知坐标系或非落地模型。
- 整屋工作台物件面板增加家具库：切换商品/本任务作品、分页、冻结、显式加入当前房间草稿，然后保存形成版本。冻结家具尺寸和材质只读；保存端复核归属、一致性及落地 Y=0，AI 不能凭空创建引用或改变冻结属性。读取失败使用线框占位并可重试，不能冒充正常家具模型。
- 复用确定性渲染器，不再次缩放毫米或全局比例；开放几何按冻结预览中心校正为整屋底面坐标。模型仍为现有参数规则表达，包围盒检查不代表精细几何、承重或制造可行性；当前只接 floor 安装，不默认支持吊装或墙装。
- 精确版本交付携带冻结资产、来源摘要和内容摘要；商品后来变更/停用后交付不漂移，全部价格仍待报价。模型可用不意味着商品已经商业核验，公开参考/开发样本不得作为真实采购证据。
- 主任务相关后端 62 项、迁移/AI 门禁 11 项通过，独立 7 项重复复核通过；前端全量 84 文件 433 项、类型检查和生产构建通过。子任务报告新增核心资产逻辑定向覆盖率 statements 99.07%、branches 86.3%、functions 97.14%、lines 100%，不代表全仓覆盖率。
- tests/home_assets_browser.cjs 在 1440/390 使用隔离 SQLite 真实 API，验证商品与开放几何分别入屋、冻结不自动保存、只读尺寸、刷新、资产 503 重试、撤销/重做、交付双资产、3D 非空和拖动变化、无页面错误及横向溢出。截图主任务已检查，报告 outputs/v2-home-assets/report.json；开放几何为显式夹具，未真实调用模型。
- 既有家装工作台和整屋户型浏览器脚本也在桌面/手机重新通过，覆盖保存失败重试、候选确认、原图鉴权、历史恢复与 3D 交互；临时 8083 验收服务已停止。
- 首次全量回归 1489 通过、2 失败、5 跳过：旧 Agent 场景用例偶现事务干扰，另一个并发测试未隔离外部模型。主任务将并发测试的事实模型输出固定为夹具；子任务新增两项确定性 RED 回归，修复业务工具执行前暂停独立检查点事务，以及 flush 失败后读取失效 task.id 掩盖原错的问题。新增回归与 Agent API 63 项、检查点专项 7 项、并发专项 7 项通过；原始偶现错误未作为单次确定复现证据。
- 最终后端全量 `python -m pytest tests -q -o addopts='' --basetemp outputs/pytest-c02-full-verified`：1493 通过、5 跳过，389.13 秒；仍有 LangChain/Pydantic v1 与 Python 3.14 兼容提示，未作为错误忽略或宣称消除。相关 Ruff、Python 编译及 `git diff --check` 通过。
- 本机开发库先备份 outputs/backups/pre_home_assets_20260917_190553.sql（4946926 字节），再 c3d4e5f6a7b8 → d4e5f6a7b8c9，仅增加资产表。已更新 8084（包含事务修复），/ready 正常且 OpenAPI 包含资产接口；8085 前端沿用，旧 8081/8082 不动。
- 下一批为精确空间版本切换的影响预览与人工处理、真实模型多轮设计验证、更多安装和使用约束，再继续预算与完整交付。正式真实案例与商业审核仍未完成，不将本批称为完整 V2 产品验收。

主要文件：backend/app/schemas/home_design_asset.py、services/home_design_asset_service.py、home_design_service.py、home_design_agent_service.py、home_design_delivery.py、api/routes/home_design.py、db/models.py 和 d4e5f6a7b8c9 迁移；frontend/src/components/homeDesign/、lib/homeAssets.ts、homeAssetLibrary.ts、useHomeAssets.ts、types/homeDesign.ts、api/homeDesignApi.ts；tests/integration/test_home_assets_independent.py、test_home_asset_registration.py、test_home_design_assets.py、unit/test_home_design_asset_guards.py、spatial_browser_server.py、home_assets_browser.cjs 及对应前端测试。回归修复另涉及 services/design_agent_service.py、langgraph_checkpoint_service.py、tests/integration/test_agent_transaction_boundaries.py 与 test_design_agent_concurrency.py。

### 2026-09-17：C02 家具冻结资产施工启动

- 本批目标：已有商品确定性家具模型和当前任务开放几何作品可冻结放入整屋，刷新、历史和来源修改后模型保持一致。先支持落地确定性规则，不接任意远程 GLB，不等同商业核验或采购报价。
- ADR-02：独立任务级不可变 HomeDesignAsset；HomeObject 只增加可空 asset_id，不接受客户端模型快照。冻结请求绑定来源类型、ID、版本与 client_mutation_id；先鉴权和重放，再检查来源当前版本。模型 JSON 不进入 Agent 上下文。
- 接口冻结为 GET /home-design/asset-options（kind=product|open_geometry、after_id、limit），POST /home-design/assets（client_mutation_id、kind、source_id、source_version），GET /home-design/assets/{asset_id}；路径均在 /api/design/tasks/{task_id} 下。列表返回 items、next_after_id；资产响应包含 id、task_id、kind、source_id、source_version、name、size、material、model_spec、content_digest。尺寸为米，模型规则保留原毫米；source_version 为整数，商品对应 record_version，开放几何对应设计 version。
- 保存必须核对资产任务归属、冻结尺寸与材质；AI 不能创建或更换资产引用，也不能修改绑定资产的尺寸和材质。用户删除后撤销仍可引用原资产。来源存在不代表已通过商业核验，交付保持待报价并携带冻结来源摘要。
- 渲染复用确定性建模组件；整屋 Y 是底面，必须明确处理开放几何中心与普通 floor_center 锚点差异。精细外观不升级现有包围盒检查为制造或安全结论。
- 当前进行中，尚无本批验收结论；后端、前端与独立测试范围分别分派，主任务负责迁移复核、集成和最终验收。未提交 Git。

### 2026-09-17：原图私有访问、并发与运行态收口

- 按主任务管理技能分派 Codex 前端复核、真实 MySQL 并发测试和独立浏览器验收；主任务核对差异、测试证据和实际服务。不启动 Gemini，不提交或推送 Git。
- 独立浏览器服务改为真实 upload 内容路由与 ProtectedUploadFiles，临时目录复制测试原图。1440/390 视口验证鉴权 Blob 原图、放大、描绘、历史恢复；旧静态地址与其他会话均被拒绝。空间及家装两套 Playwright 均通过，3D 非空且拖动改变画面，无横向溢出或页面异常。报告位于 outputs/v2-spatial-browser/report.json 与 outputs/v2-home-browser/report.json；AI 使用显式测试桩，不代表真实模型效果。
- 前端独立审查复现 A → B → A 原图切换时旧 state 可能返回已释放 Blob 地址，修复后只返回存活 URL，覆盖撤销和取消后迟到响应。补充空家装初次保存失败、刷新后沿用原幂等请求测试。前端全量 81 文件 420 项、TypeScript 与生产构建通过；仍有 vendor-three 约 667 KB 的既有分包提示。
- 家装真实本机 MySQL REPEATABLE READ 并发 2 项通过：旧快照下不同键只准一个首次保存，同键返回相同版本；测试创建记录已按精确 ID 清理。默认全量不启用真实 MySQL 写入，这两项单独显式运行。
- 后端全量首跑发现旧启动器测试要求 Vite 字面固定 8081，与当前可配置端口契约不符；只调整断言保留默认 8081、IPv4、整数范围及共同代理目标检查，启动器专项 6 项通过。最终全量 1469 项通过、5 项跳过，耗时 397.71 秒；跳过包括默认关闭的 4 项真实 MySQL 专项及 Windows 符号链接权限项。保留 Python 3.14 与 LangChain Pydantic V1 兼容警告，未将其描述为已解决。
- 主任务在 Blob 生命周期修复后再次执行空间 Playwright，1440/390 原图、恢复、描绘与 3D 交互通过；临时 8083 测试服务已停止。相关 Ruff 与差异检查通过。本轮未生成新的整体覆盖率报告，不以测试数量冒充覆盖率或设计质量。
- 已核对旧验收 API 的 PID、启动时间及命令后，仅重启 8084；8081/8082 保持不动。8085 代理已连到新版接口，8084 /ready 返回 ready，数据库/schema/存储与三个 Worker 正常。开发库保持 c3d4e5f6a7b8，备份沿用上一记录。
- 保留风险：原图静态拦截目前逐请求扫描登记记录，需后续规模化优化；派生资产未统一私有化；本轮未执行真实 LLM 建议、专业设计或授权真实住宅验收。下一批先连接真实家具资产与整屋版本，再处理空间变更影响、真实模型多轮质量及交付完整性，不用概念盒体替代精细资产或工程认证。

本轮增量文件：frontend/src/lib/usePrivateImage.ts 及其测试、homeDesignEditor.test.ts；tests/spatial_browser_server.py、spatial_workspace_browser.cjs、integration/test_home_design_mysql_concurrency.py、unit/test_startup_script.py；V2开发路线书.md、AGENTS.md。

### 2026-09-17：D01 复核与原图保护接线记录

- D01 受控建议新增持久化调用、版本绑定、局部操作、成本/限流及用户确认草稿；主任务独立跑 AI 专项 35 项通过。隔离真实 API + 显式模型桩的桌面/手机浏览器确认生成不写设计、应用只入本机草稿、保存形成新版本、旧建议禁止应用；不代表真实 LLM 效果通过。
- 开发库已先备份至 `outputs/backups/pre_home_design_20260917_170955.sql`（4942607 字节），再从 a1b2c3d4e5f6 升至 c3d4e5f6a7b8。仅新增家装与建议表；没有提交或推送 Git。
- 私有原图后端已落盘：鉴权内容接口与 UploadedImage 登记资源的静态访问阻断。主任务接入 `fetchUploadedImageContent` 与 `usePrivateImage`，原图预览和整屋底图使用会话请求头及临时 Blob；类型检查与 4 项相关测试通过。网络 503 后接线曾中断，现已修复编译，但私图浏览器回归、全量回归和开发服务重启尚未完成，不能标为产品验收通过。
- 后续必须更新隔离浏览器服务的鉴权原图路由并重跑空间原图流程；继续审查临时 Blob 释放、权限撤销、静态访问成本、真实模型建议效果及整屋商品/开放几何资产绑定。现有测试后端已停止，业务服务未被替换。

### 2026-09-17：C 编辑闭环与 E01 概念清单完成首轮复核，D01 继续施工

- 已接通 `/design/:projectId/home-design`；整屋户型仅在尺度确认、保存成功且无本机修改时提供进入入口。家装编辑与空间编辑均使用无全站页脚的工作区布局。
- C 首版支持任务级精确空间版本绑定、五类概念物件、地面/墙面/顶面材料、2D/3D、房间过滤、属性、保存/刷新恢复、撤销重做、版本历史。概念盒体不等于真实家具或设备安装模型。
- 主任务独立执行家装/交付/专项验收后端 44 项、前端相关 21 项通过。真实隔离 SQLite API 的 Playwright 在 1440px 与 390px 验证保存、503 同轮重试、恢复、撤销重做、材料修改、精确版本清单/JSON 下载及版本比较；3D 像素非空、拖动视角变化、无横向溢出。证据位于 `outputs/v2-home-browser/`，脚本 `tests/home_design_workspace_browser.cjs`；这些合成测试不替代真实案例。
- 修复实体墙碰撞，墙体按实际洞口切片检查；最多展示 200 条问题并明确截断和未完成全量检查，不能因截断返回有效。门窗检测只覆盖洞口体积，不覆盖门扇开启、通行路径与工程安全。
- E01 新增受任务权限保护的精确版本交付与比较接口，只计已指定表面净面积和物件件数。部分邻接墙按实际共线区间计量，未确认尺度不输出采购面积；所有价格保持 null/待报价。交付为概念沟通资料，无公开分享、签名或施工认证。
- D01 已启动受控 AI 建议施工：基于已保存家装及精确空间版本生成候选，白名单局部操作，几何校验后等待用户确认，不直接覆盖当前设计。主任务正在审查持久化幂等、调用期间锁释放、超时与竞态，尚未记为通过。
- 本轮没有提交 Git、没有迁移业务数据库、没有真实付费模型调用；私有原图保护、真实商品/几何资产与家装绑定、专业约束及正式真实案例验收继续保留为未完成项。

### 2026-09-17：网络恢复后重新启动主任务监管

- 主任务核对未提交家装后端草稿与整屋底座后，重新成功启动三个 Codex 子任务：`v2_home_backend` 完善 C 阶段契约与持久化/几何校验；`v2_home_frontend` 完成同一空间中的饰面与对象编辑；`v2_product_audit` 独立审查并补验收。没有启动 Gemini，没有提交 Git。
- 冻结首批 `home-design/1.0`：精确绑定 task_id 与不可变 space_version；表面 floor/ceiling/wall 和五类概念对象 furniture/equipment/lighting/textile/fixture 分开表达。尺寸单位 m，XZ 为对象中心、Y 为对象底面，旋转为度。接口保留已有草稿版本、CAS、幂等与权限语义。
- 保存允许包含明确设计问题的草稿，返回 validation.valid=false 与问题列表，不将保存成功展示为确认方案或施工安全。当前几何只表达占位对象，不能宣称精细家具、制造构造或设备工程计算完成。
- 主任务承担路由/整屋入口整合、隔离真实 API 浏览器验收、架构与文档维护；子任务按文件范围施工，未经主任务复核不记为通过。下一批优先衔接受控智能操作与版本绑定的比较/清单交付，不用页面数量替代产品闭环。
- 首轮独立审查确认的待办：任务上传静态资产的私有访问保护、HomeDesignVersion 与交付版本绑定、家装受控 Agent 操作均是产品关口。真实授权案例与专业评审仍独立保留，不承诺用合成样例完成 F 阶段。
- 本条为进行中记录，不代表 C–F 已验收；后续追加实际测试、返工和产品边界。

### 2026-09-17：阶段 2 整屋空间编辑闭环

- 遵照用户要求，不在 B02、B03 小工作包之间停止；完成空间接口、编辑产品、联调、独立复核和返修后统一交付。Codex 独立审查发现的半高墙被名称修改抬升、旧版本原图定位和异步底图覆盖问题已修复并补充验证。本轮 Codex CLI 两个施工进程因 Windows sandbox 初始化失败退出，实际界面与后续接口由主任务实现；没有再启动 Gemini。
- 新工作台位于 /design/{task_id}/space，由原设计工作台进入。支持矩形和非矩形描绘、顶点坐标修正、房间属性、共边拆分和共享墙、墙高／墙厚、门窗与通道参数；墙洞使用实际三维分段几何。2D 与 3D 共用 spatial/1.0，按整屋／房间查看，桌面三栏、手机分区切换。
- 原图保持完整比例；用户输入底图覆盖宽度后建立坐标框，实际尺寸由比例校准及明确确认保存。不能从默认尺寸、识别结果或图片自身推断可靠尺度。重绘边界明确告知删除本房间独有墙洞；共用墙体保留其他房间引用。
- 保存包含基准版本和固定幂等键；断网重试复用请求，409 保留本机草稿，422 可修正后重存。刷新恢复未保存内容，支持撤销／重做、下载／导入、历史分页和恢复为新版本，旧版本永不覆盖。读取失败不会用空文档覆盖服务器。
- 本机开发 MySQL 升级前备份 outputs/backups/pre_spatial_20260917_150904.sql（4,940,118 字节），确认完整后升级至 a1b2c3d4e5f6。新增真实 MySQL 测试验证 REPEATABLE READ 下并发首次保存与相同幂等请求，2 项通过并清理测试记录。
- 后端全量回归初跑 1380 项通过、2 项跳过，2 项旧启动器断言失败；它们仍查找已移入 manageHaus.ps1 的命令。只更新契约测试后 2 项通过，启动器实现未改。本轮最后空间／投影／权限／历史及启动器专项共 67 项通过；相关 Ruff 通过。
- 前端全量 75 文件 388 项、TypeScript 与生产构建通过；核心编辑、几何和墙洞模块覆盖率为语句 91.01%、分支 90.09%、函数 91.17%、行 92.57%。UI 浏览器覆盖不混入此统计；未生成 Python 覆盖率，不声称全部模块达到同一覆盖率。构建仍有既有 vendor-three 约 667 KB 提示。
- tests/spatial_workspace_browser.cjs 使用真实空间路由及独立 SQLite，其他应用上下文显式模拟，不调用付费模型。1440 与 390 视口通过创建／描绘、底图、门窗、保存／503 重试、409 冲突恢复、历史还原、半高墙保护、异步底图防覆盖、3D 像素非空与旋转、无横向溢出检查。产物在 outputs/v2-spatial-browser/，截图已检查。
- 当前体验入口 http://127.0.0.1:8085/design/new，代理至新版实际后端 8084；8081／8082 原运行服务未替换。既有项目可从工作台进入整屋户型。未提交或推送生产部署。
- 主实现本地检查点为 22fa8b4，未推送；既有未提交工作台改动、文档整理与启动器契约修正继续保留在工作区，没有整体混入该提交。实际后端 /ready 返回 ready，数据库、schema、存储及三个 Worker 心跳正常；浏览器验收临时后端已停止。
- 验收范围为单层、直线墙、人工可校准和修正的整屋编辑产品。自动可信重建、曲面／多楼层、梁柱固定障碍的精确模型、后续设计要素与整屋交付仍需后续工作；原始 /uploads 静态资产访问评审及真实授权实测精度验收保持未完成，不以本轮工程结果替代正式业务验收。

本轮主要文件：backend/app/api/routes/spatial.py、schemas/spatial.py、services/spatial_service.py／spatial_projection.py；frontend/src/components/spatial/、lib/spatialEditor.ts／spatialGeometry.ts／spatialMeshes.ts、pages/WholeHomePage.tsx、api/designApi.ts、types/spatial.ts、router/index.tsx、Layout.tsx、RoomSourcePreview.tsx、DesignWorkspacePage.tsx、vite.config.ts，以及对应单元／集成／浏览器测试。

### 2026-09-17：第二批并行施工启动

- 用户再次授权主任务担任项目经理，按路线书自主委派、设计、返修和验收。常规工程与界面决策不需要逐项确认。
- 当前交付关口为 V2-B02：独立于历史单房间场景的任务级整屋文档、不可变版本、归属隔离、幂等与并发冲突处理。通过后接入 V2-B03 整屋编辑，不将接口完成视为产品完成。
- 后端施工由 v2_space_backend 子任务承担；主任务负责契约冻结、依赖审查、整合与独立验收。各子任务不得覆盖既有未提交工作，也不得迁移真实业务数据库或调用付费模型完成模拟验收。
- 当前拟定文档标识 spatial/1.0，米制右手系、Y 轴向上；房间多边形使用全局 XZ 坐标。空间尺度确认独立记录，不能由默认尺寸或单房间投影推导为已确认。
- 当前为施工状态，尚无本批完成或测试通过结论；后续在本节记录实际证据。
- 用户随后明确仅使用 Codex 子任务；Gemini 通道尝试因服务端 403 退出且未修改代码，后续禁止再启动。实际后端和前端分别由 Codex 子任务施工，主任务独立复核与追加验收测试。
- 前端接口客户端已通过全量 73 文件 361 项测试及 TypeScript／生产构建；本批没有界面改动，尚不代表整屋工作台的浏览器验收。
- 主任务相关后端回归 119 项通过；最终空间专项复跑 56 项通过，涵盖凹形和旋转、共边、窄交叉、重复墙段、开口边界、任务与图片归属、锁后二次归属检查、幂等、首次创建和后续编辑并发，以及隔离 SQLite 迁移升级／降级和唯一链尾。补充验证 MySQL 方言下锁后版本及幂等查询使用 FOR UPDATE 当前读，避免默认 REPEATABLE READ 旧快照；这不是实际 MySQL 并发运行证据。相关 Ruff 与差异检查通过。
- 验收边界：未执行真实 MySQL 并发、未迁移业务库、未生成 Python 覆盖率报告。新增迁移会使尚未升级的业务库无法通过既有启动预检，运行新版前须按现有备份与迁移流程升级。整屋界面和跨领域设计尚未接入，本批不声称产品级 V2 完成。

### 2026-09-16：V1 收尾与 V2 一级路线整理

- 保留两份主体文档：V1 项目阅读指南、V2 开发路线书；README 仅作为入口。
- 合并旧项目计划、上线方案、架构契约、开发数据与阶段验收说明中的有效交接信息；最新未提交工作台记录迁入 V1。
- 原计划中已经完成的工程底座归入 V1；V2 重新围绕整屋空间和家装专项划分里程碑。
- 本轮仅整理文档与讨论路线，尚未实施 V2 功能，也未重跑业务测试、查询业务数据库或完成正式业务验收。
- 后续在本节追加范围调整、里程碑进展、验证和风险；日常代理协作与简要当前状态维护在 AGENTS.md。

### 2026-09-16：纠正示例被误当成范围的问题

- 用户明确：衣柜、厨房和家具摆放只是方向举例，不能按这几个例子限定 V2 或直接排列工程。
- 将目标扩展为以整屋空间为基础的家装设计服务，增加开放的能力地图；各方向的是否纳入及设计深度仍待讨论。
- 一级路线改为产品定义、空间底座、通用设计、代表场景与协同、整屋交付和试点；撤回先前按示例固定的阶段顺序及首批场景假设。
- 同步修正 README 与 AGENTS.md 的范围摘要；V1 指南和业务代码保持原状。本轮仅校验文档链接与差异，不重跑业务测试。

### 2026-09-16：全权施工授权与初步估算

- 用户授权 Codex 自主处理未明确的产品和工程细节；据此补充执行深度、首个体验闭环、工作包与验收口径。
- 完整 V2 初步预算 12–20 周，首个体验闭环 3–5 周；包含持续测试、集中联调与返修余量，需用实际交付速度校准。
- 本轮完成范围与工期估算并核对文档，尚未开始业务代码施工；没有执行或声称通过新的功能测试。

### 2026-09-17：主任务接管与第一批空间来源恢复

- 用户授权主任务统筹架构、接口、进度、界面设计和最终验收，并允许拆分子任务。首个子任务完成后端架构审查及本批独立代码复核，提出的旧上传重放和失败识别问题均已纳入修复。
- 后端 checkpoint 返回与有效模型绑定的原图记录；已校验结果不再被旧 checkpoint 同名字段覆写。覆盖无有效模型、较新坏模型、旧状态干扰与任务隔离。
- 前端原图和模型原子恢复，缓存迁移到 v6；上传完成与缓存丢失后均可继续查看原图。失败识别保留已有空间，较小图片 ID 的上传重放和迟到 checkpoint 不回退当前来源。
- 房间资料增加完整比例原图与独立查看入口，加载失败显示明确状态；隔离画布 stacking context，防止 3D 尺寸标签遮挡工具面板。
- 新增浏览器脚本 tests/room_source_browser.cjs，使用仓库户型图片与显式模拟 API，在隔离浏览器中验证，未调用付费模型或写业务数据库。运行产物位于 outputs/v2-room-source/。
- 按 TDD 记录实际 RED／GREEN；本轮新增测试和独立实现有本地检查点提交，未推送远端。既有未提交工作台文件在原改动上增量集成，不撤销或混入无关改动。
- 最终验证：后端相关 3 文件 78 项、前端全量 72 文件 350 项通过；TypeScript、生产构建、相关 Ruff 与差异检查通过。Playwright 在 1440 与 390 宽度检查原图显示、上传、失败保留、旧图重放、清缓存恢复、图片失败、画布非空和旋转、无横向溢出；桌面同时检查尺寸标签不遮挡资料面板，截图经主任务检查。
- 覆盖率边界：新增预览组件静态测试行覆盖率 90%、分支 100%、函数 50%，图片 onError 已由浏览器交互验证但未合并到静态覆盖率；Python 环境未安装 coverage／pytest-cov，本轮未生成后端覆盖率报告。不声称本批所有层次达到 80% 覆盖率或完整 V2 产品验收通过。
- 当前前端验收服务运行于 http://127.0.0.1:8082/design/new；浏览器验收使用隔离模拟 API，正常使用仍需启动真实后端。本批没有启动、迁移或修改业务数据库。下一步为 V2-B02 的空间 schema、版本持久化与接口实现。
