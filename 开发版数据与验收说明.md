# 开发版数据与验收

2026-09-13 更新：开发版 v1 收尾验收已完成，最新实际任务为 TASK 119，详细测试结果见 `PROJECT_STATUS.md` 顶部，阅读和演示入口见 `开发版v1交接与阅读指南.md`。下文保留之前开发过程记录；本轮将统一提交代码，原始运行报告保留本地。

本轮目标为“开发版功能闭环完成”。现有家具作为开发商品，补齐正式业务所需字段，用于真实程序和模型调用验证；数据不是厂家商业核验或真实客户授权记录。

## 数据内容

- 保留现有 40 件活动家具的名称、材质、参考价格和模型尺寸。
- 库存统一模拟为 50，适用地区 CN，交期 3–10 天，价格窗口为补充时起 180 天。
- 同空间、同类别家具建立替代 SKU 引用；尺寸缺失时复用已有确定性模型规范。
- 来源标记为 `development_fixture`，审核状态保留 `draft`，不填写审核人或核验时间。
- 每件商品 `source_metadata.development_fixture.original` 保存补充前字段。重复执行不覆盖后续编辑；恢复时检测版本冲突。
- 24 个开发房间案例覆盖客厅、卧室、餐厅、书房及全部 40 个活动 SKU。房间尺寸、门窗、需求和预算为构造数据，报告显式使用 `synthetic`，不写真实案例治理库。

## 开发运行

本机 `.env` 已启用 `DEVELOPMENT_CATALOG_ENABLED=true`。只有 `APP_ENV=development` 允许启动这一模式；关闭后开发商品不能进入选品或交付。商业就绪度和正式受保护评测继续严格拒绝开发样本。

本机文本／视觉模型单价设置为输入 1、输出 2 元／百万 token，**仅作为预算门禁仿真参数，不是供应商实际报价或账单**。正式试用前必须替换为对应供应商实际配置，不应依据本轮模拟成本做商业判断。

在 `backend` 目录执行：

```powershell
python -m app.services.development_catalog_seed
python -m app.services.development_catalog_seed --apply
python -m evals.development_catalog_cases --output ../outputs/development-catalog-report.json
```

第一条仅预览并回滚；第二条实际补充且幂等。需要恢复且确认无后续编辑时，先用 `--restore` 预览，再加 `--apply` 执行。公开参考和非活动记录不受本轮补充影响。

## 验收边界

开发评测复用目录门禁、报价复算、布局生成／修复与场景校验，报告在 `outputs/development-catalog-report.json`。这份报告不证明自然语言理解准确率、真实客户满意度或制造可行性。

`tests/development_live_acceptance.py` 是显式在线验收入口，需要 API、Generation Worker 和本地模型代理运行，会产生真实模型调用。支持恢复脚本创建的本机开发任务，避免重复创建方案。方案、分享和 PDF 使用不可变版本；开发交付带有模拟数据提示。

本轮模型任务为 TASK 63，交付检查记录在 `outputs/development-delivery-acceptance.json`。正式案例收集、厂家核验和受保护发布评测仍作为后续真实业务验收工作，不能通过更改开发标识跳过。

## 本轮实际结果

- 已对本机 40 件活动商品应用补充，17 件非目标商品未修改；开发资格可用 40 件，商业资格仍为 0 件。
- `outputs/development-catalog-report.json`：24/24 案例通过，覆盖全部 40 件家具；执行实际选品门禁、报价和布局编译，没有用手填指标代替运行。
- TASK 63 的 Generation Worker 真实调用模型，生成 3 套方案；首个不可变方案版本为 15。分享与 PDF 成功，房间场景 4 包含 4 件家具，布局没有错误或警告。
- `outputs/development-geometry-acceptance.json`：真实模型创建边几 v1，第二轮将整体比例从 `[1, 1, 1]` 改为 `[1.1, 1, 1]`，材质与全部部件保持一致；撤销生成 v3，还原 v1，重新读取后状态一致。
- 分享页面已通过浏览器检查，显示家具清单、预算与开发数据提示。公开链接按既有 24 小时有效期失效后，需要重新创建。
- 本轮修复了长家具可通过正交旋转放入房间却被判越界的问题；修复只计算原尺寸下的可行朝向，不缩小商品或按 SKU 特判。
- 发现本机成本账户和账本仍遗留指向任务的外键，造成建模事务与独立计费事务自阻塞。前向迁移 `e5a6b7c8d9e0` 已移除两条遗留约束，保留账户关系、数据和成本独立事务；新增迁移回归与实际 MySQL 双连接验证。备份位于 `outputs/backups/houseproject_pre_e5a6b7c8d9e0_20260911_175352.sql`。

## 主要改动文件

| 范围 | 文件 |
| --- | --- |
| 开发数据补充与评测 | `backend/app/services/development_catalog_seed.py`、`backend/evals/development_catalog_cases.py` |
| 资格及冻结策略 | `backend/app/core/config.py`、`backend/app/services/catalog_service.py`、`product_eligibility.py`、`frozen_product_eligibility_service.py`、`plan_traceability_audit_service.py`、`backend/app/schemas/product_eligibility.py` |
| 交付提示 | `backend/app/services/plan_delivery_service.py`、`share_service.py`、`pdf_service.py`、`backend/app/schemas/shares.py`、`backend/app/api/routes/proposal.py`、`frontend/src/pages/SharePage.tsx`、`frontend/src/api/shareApi.ts` |
| 商品运营标记 | `backend/app/api/routes/products.py`、`frontend/src/types/furniture.ts`、`frontend/src/lib/furnitureDataOrigin.ts`、`frontend/src/components/admin/ProductFormModal.tsx` |
| 稳定性修复 | `backend/app/services/layout_repair.py`、`aggregate_lock_service.py`、`backend/migrations/versions/e5a6b7c8d9e0_remove_legacy_model_cost_task_fks.py` |
| 验收 | 根目录 `tests/unit/test_development_*`、`tests/integration/test_model_cost_legacy_fk_migration.py`、`tests/integration/test_model_call_governance_mysql_concurrency.py`、`tests/development_live_acceptance.py` 及关联回归 |

本轮未提交或推送 Git，未新增依赖。开发数据和运行报告可用于继续迭代功能；正式试用前再补商业核验、真实案例及真实价格配置。

## 后续总体验收纠偏

总目标复核发现 TASK 63 的房间宽度曾采用默认 4.6 米，而需求为 4.8 米；其历史记录不覆盖。共享布局桥接已修复，新的 TASK 83 / scene 7 实际保持需求的 4.8 × 5.6 米与 2.8 米层高，三套方案及交付通过，报告为 `outputs/development-room-dimensions-acceptance.json`。在线脚本现会断言输入尺寸与场景一致，避免只验证生成出的房间内部无碰撞。

Worker 的模型调用中断恢复增加保守停止：存在已提交的模型调用预留或成本时不再跨 attempt 自动重发，返回明确终态供检查后手动重试。完整原阶段验收范围及仍缺的正式证据见 `阶段1至4验收证据核对.md`。
