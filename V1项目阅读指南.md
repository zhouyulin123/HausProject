# V1 项目阅读指南

更新日期：2026-09-16。本文是 V1 的统一阅读、启动和交接入口；后续建设只在 [V2 开发路线书](./V2开发路线书.md) 维护。

## 1. V1 到哪里结束

V1 按“开发版功能闭环”收尾：自然语言需求、方案生成、商品搭配、确定性报价、单房间三维编辑、定制家具草稿、开放几何编辑、版本恢复和方案交付已具备工程基础。

这里的家具成果是参数化设计与概念建模，不代表制造级 CAD、拆单下料或承重验证已经完成。家装已有空间识别和房间布局基础，但还没有完整的整屋户型设计产品。开发版完成也不等于原正式业务验收通过。

| 能力 | V1 已有成果 | 继续建设的边界 |
| --- | --- | --- |
| 需求与 Agent | 多轮补充、纠正、撤回、检查点恢复、幂等调用、受控工具与审批 | 自然语言准确率仍需真实案例评价 |
| 家具设计 | 柜体／桌类参数模型，开放几何、多轮编辑、撤销、放入场景 | 衣柜空间适配、内部功能设计、工艺与制造规则需要继续完善 |
| 空间识别 | 户型图／照片识别，RoomModel 可包含多个房间，带置信度和尺寸校准 | 多房间识别结果不等于整屋可编辑模型 |
| 3D 与布局 | 单房间场景、家具移动旋转、边界与碰撞校验、版本恢复 | 缺少统一整屋坐标、共享墙体、房间连接和跨房间一致性 |
| 家装专项 | 已有衣柜用途和岛台用途的基础家具规格 | 岛台不等于厨房系统；墙地顶、厨柜、设备与点位协同尚待建立 |
| 运行与交付 | 三类持久化 Worker、成本账本、版本化报价、分享、PDF、渲染接口 | 渲染能力与真实业务质量需分别验收 |

2026-09-14 的工作台整理还明确保留了两个缺口：原图 URL 尚未进入工作台持久化契约；空间校准到正式搭配的完整状态衔接仍待实现。它们是 V2 空间主线的前置任务。

## 2. 启动项目

### 环境与安装

- Node.js 20–24、npm 10 及以上；前端依赖以锁文件为准。
- Python 3.12 是 CI 基线；运行时使用安装了后端依赖的同一解释器。
- MySQL 8；数据库结构通过 Alembic 管理。
- Blender、Stable Diffusion 按渲染功能配置；基础 API 可不调用它们，但完整开发环境仍需启动三个 Worker。

从仓库根目录执行：

```powershell
Copy-Item .env.example .env
npm --prefix frontend ci
py -3.12 -m pip install -r backend/requirements-dev.txt
```

复制环境文件仅用于首次安装，已有 `.env` 时保留原配置。按 [.env.example](./.env.example) 设置数据库、文本／视觉模型连接、模型单价、登录配置和渲染选项。前端显式 Demo 的配置参考 [frontend/.env.example](./frontend/.env.example)。不要提交密钥、数据库备份或用户图片。

GPU 效果图按需安装 `backend/requirements-gpu.txt`；PyTorch 与 CUDA 需匹配实际运行环境。

### Windows 统一启停

```powershell
.\startHaus.bat --check
.\startHaus.bat
.\startHaus.bat --status
.\stopHaus.bat
```

以上是各项操作示例，停止命令在需要关闭服务时单独执行。`startHaus.bat --stop` 也可停止服务。启动器自动选择解释器，必要时通过 `HAUS_PYTHON` 指定。

- 设计入口：[http://127.0.0.1:8080/design/new](http://127.0.0.1:8080/design/new)。
- API：[http://127.0.0.1:8081](http://127.0.0.1:8081)。
- 存活与就绪检查：`/health`、`/ready`。
- API、Generation Worker、Effect Render Worker、Blender Worker 和前端在独立隐藏进程运行；启动成功后可关闭启动窗口。
- 日志和进程记录在 `backend/.runtime/launcher/`；部分服务退出时先统一停止再启动，不能重复叠加进程。
- 旧版启动窗口管理的进程需先手动退出，再使用新版启动器接管。

启动包含数据库迁移；有业务数据时先备份。`--check` 用于环境与 schema 预检，不能代替在线功能验收。`/ready` 检查数据库、上传目录及三类 Worker 心跳；仅 API 存活不能证明生成队列有人消费。

### 独立启动与部署

前端从根目录执行 `npm --prefix frontend run dev`。后端在 `backend` 目录运行：

```powershell
python -m alembic upgrade head
python -m app.run_api --port 8081
```

三个 Worker 分别在独立终端或进程管理器中启动：

```powershell
python -m app.workers.generation_worker
python -m app.workers.effect_render_worker
python -m app.workers.blender_worker
```

生产部署按根目录 [Procfile](./Procfile) 分别监管 API 和 Worker，迁移由 release 进程执行一次。模型未配置的开发环境只适用于非 AI 功能；正式模型调用还需通过成本配置检查。

## 3. 先体验这三条链路

| 链路 | 操作 | 检查重点 |
| --- | --- | --- |
| 家具设计 | “设计圆形边几，高 500 毫米，台面直径 450 毫米，浅木色台面、黑色圆柱底座”；再要求“仅 X 方向加宽 10%”；最后撤销 | 部件身份、材质和未修改参数保留，撤销生成新版本，刷新后恢复；手机切换当前作品视图 |
| 房间搭配 | 先说“重新布置客厅”；再补充宽 4.2 米、长 5.5 米、层高 2.8 米、预算两万元、CN 地区、现代简约 | 需求不重复追问，生成进入队列，报价来自服务端，场景尺寸对应已确认需求 |
| 纠正与撤回 | “刚才的预算作废，其他需求保留”；再说“宽度量错了，是四百五十厘米” | 预算仍待确认，宽度为 4.5 米，层高和风格保留；刷新不恢复已撤回字段 |

模型调用失败应保留旧状态并明确提示；不支持的几何请求应拒绝，不能修改成另一件作品。匿名任务与会话绑定，任务 URL 不是公共分享链接。未登录时 `/api/auth/me` 的 401 是预期登录态探测。

## 4. 按调用链阅读代码

技术基础为 React／TypeScript／Vite／Zustand／Three.js，后端为 FastAPI／Pydantic／SQLAlchemy／LangGraph，数据存储为 MySQL／Alembic。

| 阅读顺序 | 位置 | 关注问题 |
| --- | --- | --- |
| 1. 入口与工作台 | [DesignStartPage.tsx](./frontend/src/pages/DesignStartPage.tsx)、[DesignWorkspacePage.tsx](./frontend/src/pages/DesignWorkspacePage.tsx)、[FurnitureDesignWorkspace.tsx](./frontend/src/components/workspace/FurnitureDesignWorkspace.tsx) | 三种设计入口怎样创建和恢复同一任务？ |
| 2. API 与项目状态 | [designApi.ts](./frontend/src/api/designApi.ts)、[useDesignProjectStore.ts](./frontend/src/store/useDesignProjectStore.ts)、[design_agent.py 路由](./backend/app/api/routes/design_agent.py) | 会话归属、client_turn_id、checkpoint 和版本如何传递？ |
| 3. Agent 与事实 | [design_agent.py 工作流](./backend/app/agents/design_agent.py)、[design_agent_service.py](./backend/app/services/design_agent_service.py)、[agent_fact_extraction_service.py](./backend/app/services/agent_fact_extraction_service.py) | 如何追问、纠错、撤回并保存原文证据？ |
| 4. 空间与布局 | [room_model.py](./backend/app/schemas/room_model.py)、[room_model_service.py](./backend/app/services/room_model_service.py)、[scenes.py](./backend/app/schemas/scenes.py)、[layout_service.py](./backend/app/services/layout_service.py) | 归一化空间事实怎样转换成单房间米制场景？ |
| 5. 商品与报价 | [catalog_service.py](./backend/app/services/catalog_service.py)、[product_eligibility.py](./backend/app/services/product_eligibility.py)、[custom_quote_evidence_service.py](./backend/app/services/custom_quote_evidence_service.py) | 哪些判断由确定性规则完成？价格来源能否复算？ |
| 6. 家具建模 | [custom_furniture.py](./backend/app/schemas/custom_furniture.py)、[open_geometry.py](./backend/app/schemas/open_geometry.py)、[open_geometry_service.py](./backend/app/services/open_geometry_service.py)、[DeterministicFurnitureModel3D.tsx](./frontend/src/components/furniture/DeterministicFurnitureModel3D.tsx) | 参数、部件 ID、单位、模型与版本怎样保持一致？ |
| 7. 异步执行 | [workers](./backend/app/workers/)、[model_call_governance_service.py](./backend/app/services/model_call_governance_service.py) | 领取、租约、重试、取消、死信和模型成本如何落库？ |
| 8. 回归与评测 | [tests](./tests/)、[backend/evals](./backend/evals/)、[真实案例工作流](./.github/workflows/real-world-release-gate.yml) | 工程测试、合成案例与真实业务证据有什么区别？ |

必须保留的契约：

- 数据库 `DesignTask.id` 是任务身份；API 必须校验会话归属，不能依赖前端本地 ID 判断权限。
- `POST /api/design/tasks/{task_id}/agent-turns` 使用稳定 `client_turn_id`；重复请求不重复执行模型与写入。
- 场景和 checkpoint 采用版本并发控制，旧版本写入返回冲突；本轮完成不代表项目不能继续编辑。
- `RoomModel` 是感知与校准事实，`SceneDocument` 当前为单房间米制快照；场景 schema 已有 1.0／1.1，不应照搬旧计划中的固定版本描述。
- 房间模型可包含多个房间，但现有转换对每个房间单独居中缩放；直接把各房间画布拼起来不能保持整屋位置和相邻关系。
- 模型负责理解与规划；受控工具执行，确定性规则校验。正式方案模型失败不能静默回退模板；Demo 和开发数据必须显式标注。
- 方案、商品、价格、渲染与交付均绑定版本。已持久化模型调用预留或成本后的未知执行结果不能自动跨 attempt 重发，需按受控失败处理。
- 人工审批不绕过空间硬约束、商业资格和施工风险门禁；不返回模型思维链。

## 5. 开发数据与正式验收

以下数字是历史验收记录，本次文档整理没有重新查询数据库：

- 2026-09-11 对 40 件活动商品补充了模拟库存 50、CN 地区、3–10 天交期及补充时起 180 天价格窗口；17 件非目标商品未修改。
- 数据来源为 `development_fixture`，审核仍为 `draft`。模拟价格／库存／地区字段不代表厂家商业核验。
- 开发目录仅允许在 `APP_ENV=development` 且 `DEVELOPMENT_CATALOG_ENABLED=true` 时使用；正式商品资格和受保护评测拒绝开发证据。
- 原字段保存在 `source_metadata.development_fixture.original`；补充幂等，恢复会检查版本冲突。
- 24 个开发案例覆盖四类空间和全部 40 件活动家具，标记为 synthetic；不能充当授权真实案例。
- 历史文本／视觉模型单价曾使用输入 1、输出 2 元／百万 token 的仿真值，只用于开发预算门禁，不能用于实际商业成本结论。

开发数据工具在 `backend` 运行：

```powershell
python -m app.services.development_catalog_seed
python -m app.services.development_catalog_seed --apply
python -m evals.development_catalog_cases --output ../outputs/development-catalog-report.json
```

第一条只预览；第二条才修改数据库。恢复用 `--restore` 预览，确认版本无冲突后再显式加 `--apply`。

正式业务验收仍需至少 20 个去重、脱敏、获授权且人工标注的真实案例，development／regression／blind 三组均非空；还需独立审核的商品、真实模型单价、设计师评审、受保护环境与 runner、独立签名密钥、同提交 SHA 的候选／基线证据。仓库存在工作流不代表远端环境已经配置完成。

保留原正式目标的证据口径：关键需求字段准确率至少 95%、低置信空间事实确认率 100%、有效 SKU 与报价复算一致率 100%、真实案例布局通过率和生成成功率至少 95%，严重跨用户访问与无限重试问题为 0。它们是待验证目标，不能用本地测试通过数、合成案例成绩或单次生成成功代替；V2 新增领域还需单独定义质量指标。

## 6. 验证与历史证据

后端测试统一放在根目录 `tests/`；前端测试与源码旁置。先执行改动对应的最小回归，再按影响扩大范围。根目录示例：

```powershell
python -m pytest tests/integration/test_agent_chat_facts.py -q --basetemp=outputs/pytest-facts
python -m pytest -q --basetemp=outputs/pytest-local-v1
ruff check backend/app backend/evals tests
npm --prefix frontend run typecheck
npm --prefix frontend test
npm --prefix frontend run build
```

开放几何和组合动作的离线工程评测：

```powershell
$env:PYTHONPATH = "backend"
python -m evals.run_open_geometry_eval --output outputs/open-geometry-eval.json
python -m evals.run_agent_action_plan_eval --output outputs/agent-action-plan-eval.json
```

在线验收 `python tests/development_live_acceptance.py --qa --qa-facts-only --output outputs/development-qa-local.json` 需要运行中的 API 和模型服务，会产生实际模型调用，不能作为无费用单元测试执行。

浏览器脚本为 `tests/development_browser_acceptance.cjs`、`tests/furniture_workspace_browser.cjs`、`tests/room_workspace_browser.cjs`。需要脚本要求的 Chrome、Playwright 和 sharp；运行前查看各脚本环境配置。模拟上传响应和移动视口不能证明真实识别准确率或真机触控表现。

| 历史日期 | 记录 | 证据限制 |
| --- | --- | --- |
| 2026-09-11 | TASK 83 的场景为 4.8 × 5.6 米、层高 2.8 米；修复需求到布局的尺寸传递 | 原 TASK 63 的错误尺寸版本不覆盖；报告在 `outputs/development-room-dimensions-acceptance.json` |
| 2026-09-11 | 真实 Worker 子进程在本地 HTTP 模拟供应商收到请求后被终止，SQLite／隔离 MySQL 恢复均未重复发请求 | 见 `tests/integration/test_generation_worker_process_crash.py`，不代表真实供应商账单或生产故障演练 |
| 2026-09-13 | 后端 1314 项、前端 69 文件／339 项、TypeScript、构建、Ruff、两项工程评测及 24/24 开发案例通过 | 历史全量基线，本次未重跑；当时未生成后端覆盖率报告 |
| 2026-09-13 | TASK 119 实际生成三套方案、分享和 PDF；家具编辑及撤销恢复通过 | PDF 未包含效果图，不证明该次效果图生成已验收 |
| 2026-09-14 | 前端更新到 70 文件／343 项通过，工作台桌面和移动视口验证 | 后续工作台记录见下节；房间上传部分使用模拟响应 |

2026-09-13 记录的数据库唯一 head 为 `e5a6b7c8d9e0`，当前以迁移目录与预检为准。迁移前备份保存在本地 `outputs/backups/`。历史非阻断提示包括 Python 3.14／Pydantic v1 兼容提示、Three.js 分包约 667 KB 和 Vitest 测试环境提示；本次不重新认定这些告警的现状。

## 7. 专项运行说明

以下是贴近实现的说明，不再另立项目路线：

- [Generation Worker](./backend/GENERATION_WORKER.md)
- [Effect Render Worker](./backend/EFFECT_RENDER_WORKER.md)
- [Blender Worker](./backend/BLENDER_WORKER.md)
- [商品目录生命周期](./backend/CATALOG_LIFECYCLE.md)
- [数据目录](./backend/data/README.md)
- [真实案例目录](./backend/evals/cases/real_world/README.md)

## 8. 最新工作台变更记录

以下两项从整理前尚未提交的状态记录原样迁入，保留既有改动、验证范围和已知缺口。

## 2026-09-14 商品搭配与房间设计布局统一

- 两个入口改为宽对话栏 + 当前空间方案；手机改为对话/当前方案双视图。执行记录折叠，审批仍可见，仅有方案版本时显示确认方案操作。
- 房间资料、家具清单、预算明细通过工具栏展开，同一个 Inspector 保留挂载及搜索/上传状态；无空间、无方案时展示明确的房间导入入口，不显示默认空房间作为已生成结果。
- 工作台中的房间画布隐藏第二个 Scene Agent 对话入口，统一使用左侧对话；保留移动、旋转、视角和渲染操作。手机画布改为方形比例，其他页面原有画布行为不变。
- 验证：前端 70 个文件共 343 项测试通过，TypeScript、生产构建及差异检查通过。Playwright 新建测试任务 132/133，使用明确的模拟上传响应验证工具切换、空间待确认、画布像素非空、旋转、桌面与手机无横向溢出；截图位于 `outputs/room-workspace/`，不能作为真实识别准确率或真机触控验收证据。
- 修改文件：`frontend/src/pages/DesignWorkspacePage.tsx` 及对应测试、`frontend/src/components/workspace/DesignWorkspaceInspector.tsx`、`frontend/src/components/chat/ChatPanel.tsx`、`frontend/src/components/design/RoomView3D.tsx`；新增 `tests/room_workspace_browser.cjs`。
- 边界：原图 URL 尚未进入工作台持久化契约，暂未提供原图/模型切换；空间校准到正式搭配的完整状态衔接仍待后续实现。未改变后端算法、商品准入或报价门禁，未提交 Git。未新增全量覆盖率报告。

## 2026-09-14 家具定制工作台交互整理

- 设计入口按钮按用户目标命名为“开始搭配”“开始设计家具”“开始设计房间”；统一“最近的设计”“新建设计”“返回设计首页”以及加载、失败提示，移除入口中的项目/后端术语。同步调整页面回归测试与浏览器脚本定位，不改变接口、数据模型或创建流程。

- 根据体验反馈再次加宽桌面对话栏：从最高 380px 改为占可用宽度 38%，限制为 420–600px；手机双视图不变。同步补充工作台栏宽回归断言，未提交 Git。

- 家具定制使用独立的双栏会话与当前作品布局；整屋搭配与房间重建保持原流程，家具页面不再混入整屋方案反馈。执行记录折叠展示，审批仍保持可见。
- 对话作品与模板定制显式切换，版本工具和模板表单按需展开；收起工具保留已输入模板草稿。开放几何显示真实服务端版本，无房间时不展示无效的放置坐标。
- 家具对话使用三行输入区与尺寸、材质、造型草拟操作，不再自动发送整屋快捷指令。移动端使用对话/当前作品双视图，支持窄屏旋转、全屏查看和 Escape 退出。
- 验证：前端 69 个测试文件共 340 项通过，TypeScript 与生产构建通过；真实模型任务 124 已通过模式切换、模板输入保留、全屏、刷新恢复和桌面/手机画布像素与旋转检查。截图位于本地 `outputs/furniture-workspace/`，脚本为 `tests/furniture_workspace_browser.cjs`。手机画布已修正为固定视口高度，并新增高度断言后复测通过。
- 覆盖率边界：新布局组件的静态渲染测试行覆盖率为 50%，尚未达到技能建议的 80%；浏览器已执行模式、工具、全屏及移动视图交互，但没有合并到该单元覆盖率报告，不宣称已满足覆盖率门槛。
- 涉及文件：新增 `FurnitureDesignWorkspace.tsx` 与根目录浏览器验收脚本；调整 `DesignWorkspacePage.tsx`、`ChatPanel.tsx`、`OpenGeometryPanel.tsx`、`FurnitureModelViewer.tsx`，更新工作台和开放几何面板测试及本状态记录。
- 本轮不修改后端几何建模规则，不声明解决模板造型质量、制造或报价问题；未提交 Git，保留已有启动脚本及 AGENTS.md 修改。手机验证为桌面浏览器模拟视口，不等同于真机触控验收。
