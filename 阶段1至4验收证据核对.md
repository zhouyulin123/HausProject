# 阶段 1–4 验收证据核对

核对时间：2026-09-11。依据《AI家装智能体上线开发方案》原阶段目标；开发版数据授权不替代正式业务证据。上一轮 1265 项后端、339 项前端测试通过属于工程回归证据，不单独证明全部业务验收达标。

## 当前结论

开发商品已可运行完整链路，原正式目标尚未完成。真实治理库只读结果：候选 0、冻结 0、三个切分准入均为 0；商业商品资格 0，开发资格 40。仓库存在受保护发布工作流，但本轮未验证远端 runner、密钥或分支 required check，不能据 YAML 宣称发布验收通过。

实际复核发现 TASK 63 的确认需求是宽 4.8 米，而场景 4 使用默认宽 4.6 米。24 条目录案例直接将场景几何传给布局器，未覆盖任务需求到场景的桥接，因此其 24/24 成绩不能证明这条传递链路正确。本轮已修复该缺口并增加在线尺寸断言：新的 TASK 83 真实模型生成 3 套方案，场景 7 保持 4.8 × 5.6 米、层高 2.8 米，布局、分享及 PDF 通过，见 `outputs/development-room-dimensions-acceptance.json`；旧不可变版本仍保留原始记录。

恢复审查另发现原 Worker attempt 改变后可能重复调用模型。本轮在租约恢复、普通异常重试及旧排队任务认领三个入口增加保守拦截：只要已成功持久化调用预留（含零金额）或已有成本，就停止自动重发并进入明确 dead_letter；调用前失败仍可恢复，取消保持优先，同一活跃 attempt 内的有界业务重规划不受影响。新增及相关 5 个测试文件 77 项通过。

随后新增 `tests/integration/test_generation_worker_process_crash.py`：真实 Worker 子进程经 SDK 向本机模拟供应商发送 HTTP，供应商收到请求后阻塞；终止 Worker，等待真实 30 秒租约到期，再启动新的 Worker。SQLite 与隔离 MySQL 两种数据库均通过：供应商请求数保持 1、attempt 保持 1、run 为 dead_letter、账本仍为 reserved、预留成本保留。没有修改时间或用异常代替进程终止，MySQL 临时库已清理。该证据覆盖本地实际进程／HTTP／数据库边界；真实供应商账单、生产部署故障注入仍需试点环境验收。

## 阶段 1–2 的验收范围

| 阶段与验收 | 已检查的工程证据 | 证据边界 |
| --- | --- | --- |
| 阶段 1：缺预算／尺寸暂停 | `test_design_agent.py` 的最少追问、`test_async_generation_api.py` 的不完整事实拒绝、`test_design_agent_api.py` 的低置信事实审计确认 | 直接调用布局桥接的尺寸来源缺口在本轮修复；不能只用 Agent 追问测试代替几何校验。 |
| 阶段 1：最多两次预算重规划，否则转人工 | `test_task_generation_failure.py`、`test_generation_worker.py` 覆盖超预算后成功、重规划耗尽与 needs_human | 模型回复为受控测试输入；真实预算重规划样本基线尚未建立。 |
| 阶段 1：非法 SKU／报价／空间不完成 | `test_design_agent.py` 验证负报价及三次执行后转人工；Worker 硬布局错误回归；目录生命周期回归 | 24 个开发案例和 TASK 63 是补充运行证据，不代表所有真实户型通过。 |
| 阶段 1：关闭后检查点恢复 | `test_design_agent_checkpoint_resume.py` 覆盖工具前／后的恢复和幂等；开放几何实际 GET 恢复一致 | 不把 GET 一致性等同于完整浏览器关闭重开验收。 |
| 阶段 1：步数、重试、成本和退出原因 | Agent max_steps/max_retries、模型预留账本和失败时间线；本轮 MySQL 双连接成本测试 | 本机单价为仿真值，实际计费金额未验证。 |
| 阶段 2：Worker 中断恢复或明确失败 | 租约／截止时间／取消回归；`test_generation_worker_process_crash.py` 在 SQLite 和 MySQL 实际杀进程后重新启动 Worker，明确终止且不重发 HTTP | 本地供应商模拟服务，不等于实际供应商账单或生产基础设施演练。 |
| 阶段 2：重复操作不重复生成／计费 | API 幂等、账本唯一键、Worker 预留、开放几何实际同键重放 | 外部供应商未知计费由保守预留处理，不保证供应商端未知请求退款。 |
| 阶段 2：完整时间线与成本 | GenerationRun、TaskExecutionEvent、ModelCallLedger 及质量摘要；TASK 63 实际调用记录 | 仿真价格不能作为商业成本基线。 |
| 阶段 2：供应商故障不假成功、不永久卡住 | 熔断、超时、dead_letter、停止模板假成功的回归 | 本轮未执行完整生产等价供应商故障演练。 |

## 阶段 3 的五项验收

| 验收项 | 当前证据与缺口 |
| --- | --- |
| 线上无效 SKU 率 0 | `catalog_service.py`、`product_eligibility.py` 执行硬过滤；开发案例通过。正式线上样本不足，不能宣布总体率为 0。 |
| 报价复算一致率 100% | `custom_quote_evidence_service.py` 和冻结商品证据按快照复算；相关回归通过，开发案例 24/24。正式样本统计缺失。 |
| 异常价格、库存和有效期不能静默推荐 | `test_catalog_lifecycle.py` 验证失效、库存不足、替代和无替代失败；`test_development_catalog.py` 验证开发策略仍执行硬约束。 |
| 20 个方案由设计师追溯商品和价格 | `plan_traceability_audit_service.py` 只接受签名 `production_acceptance` cohort；当前没有 20 个正式方案与设计师验收证据。开发样本不冒充该 cohort。 |
| 资产失败明确降级 | `product_asset_service.py` 与资产接口测试验证授权、路径和审核状态；TASK 63 场景显式保留 parametric 与 fallbackReason。不是家具数字孪生。 |

## 阶段 4 的八项首期指标

| 指标 | 原目标 | 目前可以证明什么 |
| --- | --- | --- |
| 关键需求字段准确率 | ≥95% | 有标注与可信复算工具；没有合格真实样本，准确率未测。 |
| 低置信空间事实人工确认率 | 100% | `test_low_confidence_room_facts_require_audited_confirmation` 覆盖工程路径；真实样本分母为 0。 |
| 有效 SKU 率 | 100% | 开发商品门禁已验证；正式业务指标未测。 |
| 报价复算一致率 | 100% | 开发样本与规则回归通过；正式业务指标未测。 |
| 真实案例布局硬约束通过率 | ≥95% | 24 个构造布局通过；无真实布局集，不能替代该指标。 |
| 方案生成成功率 | ≥95% | TASK 63 实际 Worker 生成成功；单个任务不足以证明真实总体成功率。 |
| 严重跨用户访问问题 | 0 | 归属、角色与匿名会话相关集成测试通过；受保护环境独立安全证明本轮未核验。 |
| 无限循环或无限重试 | 0 | 步数、重规划、租约、成本及截止时间回归通过；真实运行分布与受保护证据仍需补齐。 |

## 质量飞轮与发布证据

1. 数据治理与切分：`real_world_governance_service.py` 检查授权、脱敏、标注、用途、去重和三组冻结；实际无可冻结记录。
2. 指标：`trusted_evidence.py`、`trusted_output_metrics` 相关测试拒绝手写分数、快照篡改及无独立证据的安全指标；满意度、修改率需要真实反馈，不能填假基线。
3. 反馈：匿名采用、删除、替换、移动、最终选择与满意度进入质量摘要；空样本不填默认高分。
4. 版本对比：质量摘要按模型、Prompt、规则及数据摘要精确分组；缺少版本信息明确保留缺失状态。
5. 回归发布：`.github/workflows/real-world-release-gate.yml` 要求目标 SHA、受保护环境和 `[self-hosted, real-world-eval]` runner；普通 PR 仅验脱敏 proof。当前未运行该正式门禁。
6. 评分边界：关键事实依靠冻结输入与确定性复算或人工标注，LLM Judge 不替代它们。
7. 失败簇：管理员只能推进到 resolved；三组候选／基线证明经签名核验后才可 verified，复发重开。工具有回归，尚无真实样本飞轮运行结果。

## 后续输入

正式验收需要至少 20 个获授权、脱敏、人工标注并划分 development/regression/blind 的案例；真实厂家商业字段及独立审核；设计师来源追溯和满意度基线；受保护运行环境、独立密钥、目标提交与候选／基线实际证据。本轮不上传 Git，不触发外部发布，也不通过改数据标签绕过这些要求。
